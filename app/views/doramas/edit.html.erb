<h1>投稿を編集</h1>

<!--doramas_pathではなくdorama_pathでした-->
<!--localhost:3000/rails/info/routesでroutes見れるんやけど、そこで見たら登録はdorama_pathで更新がdorama_pathってことがわかる。-->
<!--あと、resourcesでroutesを自動生成した場合はmethodは決められてるから指定しなくてもいい-->

<%= form_for @dorama, url: dorama_path do |f| %>
  <% if @dorama.errors.any? %>
    <div id="error_explanation">
      <h2><%= @dorama.errors.count %>件のエラーが発生したため保存ができませんでした。</h2>
      <ul>
      <% @dorama.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :story, "あらすじ" %><br>
    <%= f.text_field :story %>
  </p>

  <p>
    <%= f.label :title, "タイトル" %><br>
    <%= f.text_area :title, cols: 60, rows: 8 %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>

<%= link_to 'Show', @dorama %> |
<%= link_to '戻る', doramas_path %>



